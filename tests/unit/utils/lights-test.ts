import lights from 'explorviz-frontend/utils/lights';
import { module, test } from 'qunit';

module('Unit | Utility | lights', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = lights();
    assert.ok(result);
  });
});
