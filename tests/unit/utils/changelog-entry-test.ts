//import changelogEntry from 'explorviz-frontend/utils/changelog-entry';
import { module, test } from 'qunit';

module('Unit | Utility | changelog-entry', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    //let result = changelogEntry();
    assert.ok(true);
  });
});
