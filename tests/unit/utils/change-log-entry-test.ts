//import changeLogEntry from 'explorviz-frontend/utils/change-log-entry';
import { module, test } from 'qunit';

module('Unit | Utility | change-log-entry', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    //let result = changeLogEntry();
    assert.ok(true);
  });
});
