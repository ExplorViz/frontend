import scene from 'explorviz-frontend/utils/scene';
import { module, test } from 'qunit';

module('Unit | Utility | scene', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = scene();
    assert.ok(result);
  });
});
