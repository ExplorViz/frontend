<div class='popover-body'>
  <ul class='nav nav-tabs justify-content-center' role='tablist'>
    <li class='nav-item' role='presentation'>
      <button
        class='nav-link active'
        id='runtime-tab-{{this.htmlIdUnique}}'
        data-toggle='tab'
        data-target='#runtime-{{this.htmlIdUnique}}'
        type='button'
        role='tab'
        aria-controls='runtime-{{this.htmlIdUnique}}'
        aria-selected='true'
      >Runtime</button>
    </li>
    <li class='nav-item' role='presentation'>
      <button
        class='nav-link'
        id='code-tab-{{this.htmlIdUnique}}'
        data-toggle='tab'
        data-target='#code-{{this.htmlIdUnique}}'
        type='button'
        role='tab'
        aria-controls='code-{{this.htmlIdUnique}}'
        aria-selected='false'
      >Code</button>
    </li>
    {{#if @restructureMode}}
      <li class='nav-item' role='presentation'>
        <button
          class='nav-link'
          id='restructure-tab-{{this.htmlIdUnique}}'
          data-toggle='tab'
          data-target='#restructure-{{this.htmlIdUnique}}'
          type='button'
          role='tab'
          aria-controls='restructure-{{this.htmlIdUnique}}'
          aria-selected='false'
        >Restructure</button>
      </li>
    {{/if}}
  </ul>
  <div class='tab-content'>
    <div
      class='tab-pane fade show active'
      id='runtime-{{this.htmlIdUnique}}'
      role='tabpanel'
      aria-labelledby='runtime-tab-{{this.htmlIdUnique}}'
    >
      {{yield to='runtimeTab'}}
    </div>
    <div
      class='tab-pane fade'
      id='code-{{this.htmlIdUnique}}'
      role='tabpanel'
      aria-labelledby='code-tab-{{this.htmlIdUnique}}'
    >
      {{yield to='codeTab'}}

    </div>
    {{#if @restructureMode}}
      <div
        class='tab-pane fade'
        id='restructure-{{this.htmlIdUnique}}'
        role='tabpanel'
        aria-labelledby='restructure-tab-{{this.htmlIdUnique}}'
      >
        {{yield to='restructureTab'}}
      </div>
    {{/if}}
  </div>
</div>