<Collaboration::AutoSelectLandscape @landscapeToken={{this.landscapeToken}} />
<div id='ember-right-click-menu-wormhole'></div>
<div id='main-container'>
  {{#if this.user}}
    <PageSetup::Navbar />
    <div {{react this.toastMessage}} />
  {{/if}}
  {{outlet}}
</div>