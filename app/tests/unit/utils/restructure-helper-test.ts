//import restructureHelper from 'explorviz-frontend/utils/restructure-helper';
import { module, test } from 'qunit';

module('Unit | Utility | restructure-helper', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    //let result = restructureHelper();
    assert.ok(true);
  });
});
