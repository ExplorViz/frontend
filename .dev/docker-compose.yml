services:

  demo-supplier:
    container_name: explorviz-demo-supplier
    image: explorviz/demo-supplier:neo4j-helper
    ports:
      - ${SPAN_PORT}:8083
      - ${USER_PORT}:8084
      - ${CODE_PORT}:8085

networks:
  default:
    name: explorviz
